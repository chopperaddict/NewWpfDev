<Window x:Class="Views.Genericgrid"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Views"
        xmlns:users="clr-namespace:UserControls"
        mc:Ignorable="d"
     WindowChrome.ResizeGripDirection="BottomRight"
	    ResizeMode="CanResizeWithGrip"
        Loaded="IsLoaded"
           Title="Genericgrid" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="BankAccountGridStyle" TargetType="DataGridCell" BasedOn="{StaticResource {x:Type DataGridCell}}">
            <Style.Triggers>
                <!--// Window IS FOCUSED-->

                <!--#region Special Triggers for Generic style Tables-->
                <!--Clever stuff Eh ??-->
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsSelected}" Value="False" />
                        <Condition Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=DataGridRow}, Path=IsMouseOver}" Value="False" />
                    </MultiDataTrigger.Conditions>
                    <MultiDataTrigger.Setters>
                        <Setter Property="Background" Value="{StaticResource Green5}" />
                        <Setter Property="Foreground" Value="{StaticResource Black0}" />
                        <Setter Property="FontWeight" Value="Normal" />
                    </MultiDataTrigger.Setters>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsSelected}" Value="True" />
                        <Condition Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=DataGridRow}, Path=IsMouseOver}" Value="false" />
                    </MultiDataTrigger.Conditions>
                    <MultiDataTrigger.Setters>
                        <Setter Property="Background" Value="{StaticResource Red0}" />
                        <Setter Property="Foreground" Value="{StaticResource White0}" />
                        <Setter Property="FontWeight" Value="DemiBold" />
                    </MultiDataTrigger.Setters>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsSelected}" Value="false" />
                        <Condition Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=DataGridRow}, Path=IsMouseOver}" Value="true" />
                    </MultiDataTrigger.Conditions>
                    <MultiDataTrigger.Setters>
                        <Setter Property="Background" Value="{StaticResource Orange1}" />
                        <Setter Property="Foreground" Value="{StaticResource White0}" />
                        <Setter Property="FontWeight" Value="DemiBold" />
                    </MultiDataTrigger.Setters>
                </MultiDataTrigger>
                <!--#endregion Special Triggers for Non Bamk/Customer Tables-->

                <!--#endregion Triggers-->
            </Style.Triggers>
        </Style>

    </Window.Resources>
    <Grid Background="{StaticResource Black4}" >
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="85"/>
        </Grid.RowDefinitions>

        <users:DatagridControl x:Name="GenGridCtrl"   
            Background="{StaticResource Black4}"
            FontSize="16"
            FontWeight="Normal"
            Foreground="Red"
            Cellstyle="{StaticResource BankAccountGridStyle}"
            AlternateBackground="LimeGreen"
            Tablename="BankAccount"/>

        <ComboBox x:Name="SqlTables" 
                  Grid.Row="1" HorizontalAlignment="Right" 
                  Width="200" Height="25"  FontSize="16"
                  Margin="0,5,140,5" Background="Green" 
                   VerticalAlignment="Top"
                  SelectionChanged="SqlTables_SelectionChanged"/>
        <CheckBox x:Name="ToggleColumnHeaders" Checked="ToggleColumnHeaders_Checked" IsChecked="false"
                  Content="Toggle Column headers" HorizontalAlignment="Right" Foreground="White" FontSize="16" VerticalAlignment="Top"
                  Click="ToggleColumnHeaders_Click"  Height="20" Margin="0,3,420,0" Grid.Row="1" Grid.RowSpan="2" Width="184"/>
        <Button Height="35" Width="160" Content="Show Full Columns Schema" 
                HorizontalAlignment="Left"
                 VerticalAlignment="Top"
                Click="ShowColumnInfo_Click" Margin="5,0,0,0" 
                Background="Green" Grid.Row="1" />
        <Button Height="55" Width="110" Content="Close" 
                HorizontalAlignment="Right"
                Click="Close_Click" Margin="5,0,10,0" 
                Background="Red" Foreground="White"  Grid.Row="1" />
        <Label Content="New Table Name : " Grid.Row="1" 
                 HorizontalAlignment="Right"
               Foreground="White"  
               Width="Auto"
               FontSize="16"
                 Margin="0,50,380,6"/>
        <TextBox x:Name="NewTableName" Width="200" Foreground="White"  Grid.Row="1" 
                 HorizontalAlignment="Right"
                 Margin="0,49,140,9"/>
    </Grid>
</Window>
    
    
